# Copy to .env and adjust for your environment.

# Backend HTTP port
PORT=4000

# Set to "production" in production deployments
NODE_ENV=production

# Path to the SQLite database file (persist on a volume in production)
DB_PATH=/app/backend/data.db

# Directory for database backups (persist + back up)
BACKUP_DIR=/app/backend/backups
